---
description: ViaBariloche — workflow orquestado completo con auto-detección de tipo (plan? → inspect → spec → execute → security → review)
argument-hint: <descripción de la tarea>
---
Invocá la skill `orq` vía la tool `Skill` con este argumento: $ARGUMENTS
