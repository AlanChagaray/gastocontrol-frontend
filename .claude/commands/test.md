---
description: ViaBariloche — ejecuta los tests automatizados del proyecto SI existen (comando individual, fuera del workflow). Sin tests, informa y termina.
argument-hint: [qué validar o filtro de tests, o vacío para la suite completa]
---
Invocá la skill `test` vía la tool `Skill` con este argumento: $ARGUMENTS
