---
description: ViaBariloche — modo profesor: explica los cambios y enseña el lenguaje del cambio (auto-detecta; Python por default) con puente desde PHP. Comando individual, fuera del workflow. Solo lectura.
argument-hint: [tema puntual a explicar — o vacío para explicar el ciclo recién terminado]
---
Invocá la skill `teach` vía la tool `Skill` con este argumento: $ARGUMENTS
